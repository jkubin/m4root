# using brackets for nonterminals

# A → β
define([KEYWORDS], The keywords [[define([]), defn([]), …]] are disabled.)
define([CHARS],    A sentence with comma[,] brackets [RR()] or [LL()].)
define([COMMENTS], The [#] and [dnl] are not comments in brackets.)

divert(0)dnl
KEYWORDS
CHARS
COMMENTS
