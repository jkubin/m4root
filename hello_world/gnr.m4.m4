# M4 code to generate another M4 code

divert(0)dnl
[dnl vim:ft=m4
divert(-1)changequote([,])

# ]DONTE()[
#
# usage:
# m4 hello_world.gnr.m4

define([SYMBOL], ]LL()defn([SYMBOL])][)

divert(0)dnl
SYMBOL]
